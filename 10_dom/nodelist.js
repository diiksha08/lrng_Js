/*

forEach can be used on nodeList but map() cannot, to use map() on nodeList:
convert nodeList to Array by : Array.from("name of that nodeList")

HTMLCollection
to use forEach()/ map(), or any array method, need to convert HTMLCollection to
Array by: Array.from('name of that HTMLCollection') 
